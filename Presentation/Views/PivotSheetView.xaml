<UserControl x:Class="Presentation.Views.PivotSheetView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:sf="http://schemas.syncfusion.com/wpf"
             xmlns:behaviors="clr-namespace:Presentation.Behaviors"
             xmlns:syncfusionskin="clr-namespace:Syncfusion.SfSkinManager;assembly=Syncfusion.SfSkinManager.WPF"
             syncfusionskin:SfSkinManager.Theme="{syncfusionskin:SkinManagerExtension ThemeName=Windows11Light}"
             x:Name="Root">
    
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width=".35*"/>
        </Grid.ColumnDefinitions>
    
        <sf:PivotGridControl
            Grid.Column="0"
            x:Name="PivotGridControl"
            IsDynamicData="True"
            ResizePivotGridToFit="True"
            ItemSource="{Binding PivotData}">
        
            <i:Interaction.Triggers>
                <i:EventTrigger EventName="Loaded">
                    <i:InvokeCommandAction Command="{Binding PivotGridLoadedCommand}"/>
                </i:EventTrigger>
            </i:Interaction.Triggers>
            
            <i:Interaction.Behaviors>
                <behaviors:PivotGridControlBehavior
                    PivotColumns="{Binding PivotColumns}"
                    PivotRows="{Binding PivotRows}"
                    PivotCalculations="{Binding PivotCalculations}"/>
            </i:Interaction.Behaviors>
            
            
        </sf:PivotGridControl>
    
        <sf:PivotSchemaDesigner Grid.Column="2" 
                                PivotControl="{Binding ElementName=PivotGridControl}">
            
        </sf:PivotSchemaDesigner>
    </Grid>
</UserControl>
