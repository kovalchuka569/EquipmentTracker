<UserControl x:Class="Presentation.Views.DialogBoxView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:controls="clr-namespace:Presentation.Controls"
             Style="{DynamicResource LoadUserControlAnimationBottomToTop}">
    
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/Presentation;component/Styles/Animations/LoadUserControlAnimationBottomToTop.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
   
        <controls:DialogBox
                    Icon="{Binding Icon}"
                    Title="{Binding Title}"
                    Message="{Binding Message}"
                    Buttons="{Binding Buttons}"
                    Button1Text="{Binding Button1Text}"
                    Button2Text="{Binding Button2Text}">
            
            <i:Interaction.Triggers>
                <i:EventTrigger EventName="DialogResultSelected">
                    
                    <i:InvokeCommandAction Command="{Binding DialogResultSelectedCommand}"
                                           PassEventArgsToCommand="True"/>
                </i:EventTrigger>
                
            </i:Interaction.Triggers>
        </controls:DialogBox>
    
</UserControl>
