<UserControl x:Class="Presentation.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:controls="clr-namespace:Presentation.Controls"
             xmlns:sf="http://schemas.syncfusion.com/wpf"
             xmlns:localization="clr-namespace:Resources.Localization;assembly=Resources"
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors">
    
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Presentation;component/Resources/Icons/Icons.xaml" />
            </ResourceDictionary.MergedDictionaries>
            
            <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
            
        </ResourceDictionary>
    </UserControl.Resources>
    
    <Grid>
    
            <Grid>
                
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                
                    <controls:HoverButton
                    x:Name="RemoveMarkedItemsButton"
                    Margin="5"
                    VerticalAlignment="Top"
                    HorizontalAlignment="Left"
                    TextContent="{x:Static localization:Strings.RemovingMarkedItems}"
                    NormalIconContent="{StaticResource BroomLightLine64}"
                    HoverIconContent="{StaticResource BroomDarkLine64}"
                    NormalIconHeight="16"
                    NormalIconWidth="16"
                    HoverIconHeight="16"
                    HoverIconWidth="16"
                    ChangeIconHover="True"
                    NormalBackground="#09244B"
                    HoverBackground="#E8E8E8"
                    ContentFill="White"
                    HoverContentFill="#09244B"
                    ButtonWidth="225"
                    ButtonHeight="35"/>
                    
                    
            </Grid>
        
        <ContentControl 
            Content="{Binding OverlayContent}"
            Visibility="{Binding IsOverlayVisible, Converter={StaticResource BooleanToVisibilityConverter}}"
            Panel.ZIndex="1"/>
        
        <ContentControl 
            Content="{Binding DialogContent}"
            Visibility="{Binding IsDialogOpen, Converter={StaticResource BooleanToVisibilityConverter}}"
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            Panel.ZIndex="2"/>
        
        </Grid>
    
    
    <i:Interaction.Triggers>
        <i:EventTrigger SourceObject="{Binding ElementName=RemoveMarkedItemsButton}" 
                        EventName="MouseLeftButtonUp">
            <i:InvokeCommandAction Command="{Binding ShowRemoveMarkedItemsDialogCommand}"/>
        </i:EventTrigger>
    </i:Interaction.Triggers>  
    
</UserControl>
