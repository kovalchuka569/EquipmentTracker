<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    
    <Style x:Key="RootGridStyle" TargetType="Grid">
        <Setter Property="Background" Value="{StaticResource Neutral.Brush}"/>
    </Style>
    
    <Style x:Key="AuthorizationBorderStyle" TargetType="Border">
        <Setter Property="BorderThickness" Value="{StaticResource BorderThickness1013}"/>
        <Setter Property="BorderBrush" Value="{StaticResource NeutralDark1.Brush}"/>
        <Setter Property="CornerRadius" Value="{StaticResource CornerRadius6}"/>
        <Setter Property="Background" Value="{StaticResource White.Brush}"/>
        <Setter Property="Padding" Value="{StaticResource Padding36}"/>
        <Setter Property="HorizontalAlignment" Value="Center"/>
        <Setter Property="VerticalAlignment"   Value="Center"/>
    </Style>
    
</ResourceDictionary>