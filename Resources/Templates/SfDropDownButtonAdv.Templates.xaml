<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    
                    xmlns:sf="http://schemas.syncfusion.com/wpf">
    
    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="pack://application:,,,/Resources;component/Global/Common.xaml"/>
        <ResourceDictionary Source="pack://application:,,,/Resources;component/Global/Brushes.xaml"/>
    </ResourceDictionary.MergedDictionaries>

    <ControlTemplate x:Key="DefaultDropDownButtonAdvTemplate" TargetType="sf:DropDownButtonAdv">
        <Border x:Name="ButtonBorder"
                CornerRadius="{StaticResource CornerRadius4}"
                Background="{TemplateBinding Background}"
                BorderBrush="{TemplateBinding BorderBrush}"
                BorderThickness="{TemplateBinding BorderThickness}"
                Padding="{TemplateBinding Padding}">
            <Grid>

                <Border x:Name="ItemBorder"
                        CornerRadius="{StaticResource CornerRadius4}"
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        Padding="{TemplateBinding Padding}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <Image x:Name="Icon"
                               Grid.Column="0"
                               Margin="{StaticResource Margin2}"
                               Source="{Binding SmallIcon, RelativeSource={RelativeSource TemplatedParent}}"
                               Width="{Binding IconWidth, RelativeSource={RelativeSource TemplatedParent}}"
                               Height="{Binding IconHeight, RelativeSource={RelativeSource TemplatedParent}}" />

                        <AccessText x:Name="PART_Text"
                                    Grid.Column="1"
                                    Margin="4,6,6,6"
                                    Text="{TemplateBinding Label}"
                                    Foreground="{TemplateBinding Foreground}"
                                    FontSize="{TemplateBinding FontSize}"
                                    FontFamily="{TemplateBinding FontFamily}"
                                    HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                    VerticalAlignment="{TemplateBinding VerticalContentAlignment}" />

                        <Path x:Name="Arrow"
                              Grid.Column="2"
                              Data="M 0,0 L 4,4 8,0 Z"
                              Fill="{TemplateBinding Foreground}"
                              Margin="2,0,2,0"
                              VerticalAlignment="Center"
                              HorizontalAlignment="Center" />
                    </Grid>
                </Border>

                <Popup x:Name="PART_DropDown"
                       AllowsTransparency="True"
                       Placement="Bottom"
                       PopupAnimation="Slide"
                       StaysOpen="{TemplateBinding StaysOpen}">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{StaticResource BorderThickness1}">
                        <ContentPresenter />
                    </Border>
                </Popup>
            </Grid>
        </Border>
    </ControlTemplate>
    
    <ControlTemplate x:Key="OnlyIconDropDownButtonAdvTemplate" TargetType="sf:DropDownButtonAdv">
        <Border x:Name="ButtonBorder"
                CornerRadius="{StaticResource CornerRadius4}"
                Background="{TemplateBinding Background}"
                BorderBrush="{TemplateBinding BorderBrush}"
                BorderThickness="{TemplateBinding BorderThickness}"
                Padding="{TemplateBinding Padding}">
            <Grid>

                <Border x:Name="ItemBorder"
                        CornerRadius="{StaticResource CornerRadius4}"
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        Padding="{TemplateBinding Padding}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <Image x:Name="Icon"
                               Grid.Column="0"
                               Margin="{StaticResource Margin6}"
                               Source="{Binding SmallIcon, RelativeSource={RelativeSource TemplatedParent}}"
                               Width="{Binding IconWidth, RelativeSource={RelativeSource TemplatedParent}}"
                               Height="{Binding IconHeight, RelativeSource={RelativeSource TemplatedParent}}" />

                        <Path x:Name="Arrow"
                              Grid.Column="1"
                              Data="M 0,0 L 4,4 8,0 Z"
                              Fill="{TemplateBinding Foreground}"
                              Margin="2,0,2,0"
                              VerticalAlignment="Center"
                              HorizontalAlignment="Center" />
                    </Grid>
                </Border>

                <Popup x:Name="PART_DropDown"
                       AllowsTransparency="True"
                       PopupAnimation="Slide"
                       Placement="Bottom"
                       StaysOpen="{TemplateBinding StaysOpen}">
                    <Border Background="{StaticResource White.Brush}"
                            BorderBrush="{TemplateBinding Background}"
                            CornerRadius="{StaticResource CornerRadius4}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            Margin="{StaticResource Margin0400}">
                        <ContentPresenter />
                    </Border>
                </Popup>
            </Grid>
        </Border>
    </ControlTemplate>
    
</ResourceDictionary>