<UserControl x:Class="UI.Views.TabControl.TabControlView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             xmlns:sf="http://schemas.syncfusion.com/wpf"
             xmlns:prism="http://prismlibrary.com/"
             xmlns:syncfusionskin="clr-namespace:Syncfusion.SfSkinManager;assembly=Syncfusion.SfSkinManager.WPF"
             syncfusionskin:SfSkinManager.Theme="{syncfusionskin:SkinManagerExtension ThemeName=Windows11Light}"
             prism:ViewModelLocator.AutoWireViewModel="True">
    
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Syncfusion.Themes.Windows11Light.WPF;component/tabcontrolext/tabcontrolext.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    
    <Grid>
        
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height = "*"/>
        </Grid.RowDefinitions>
        
        <sf:TabControlExt 
            TabItemHeaderStyle="Fill" 
            Margin="0"
            Padding="0"
            CloseMode="Delete"
            EnableLabelEdit="False"
            CloseButtonType="Extended"
            DefaultContextMenuItemVisibility="Hidden"
            IsCustomTabItemContextMenuEnabled="True"
            SelectedItem="{Binding SelectedTab, Mode=TwoWay}" ItemsSource="{Binding TabItems}"
            BorderThickness="0">
            
            <sf:TabControlExt.ItemContainerStyle>
                <Style TargetType="sf:TabItemExt" BasedOn="{StaticResource SyncfusionTabItemExtStyle}">
                    <Setter Property="HeaderTemplate">
                        <Setter.Value>
                            <DataTemplate>
                                <TextBlock Text = "{Binding Header, Mode=TwoWay}"/>
                            </DataTemplate>
                        </Setter.Value>
                    </Setter>
                    
                    
                </Style>
                
            </sf:TabControlExt.ItemContainerStyle>
            
            <sf:TabControlExt.ContentTemplate>
                <DataTemplate>
                </DataTemplate>
            </sf:TabControlExt.ContentTemplate>
            
        </sf:TabControlExt>
        
        <ContentControl Grid.Row="1" prism:RegionManager.RegionName="TabContentRegion"
                        prism:RegionManager.RegionContext="{Binding}"/>
    </Grid>
</UserControl>
