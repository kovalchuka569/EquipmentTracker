<UserControl x:Class="UI.Views.TabControl.TabControlView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:UI.Views.TabControl"
             mc:Ignorable="d"
             xmlns:sf="http://schemas.syncfusion.com/wpf"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True"
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors">
    
    
    <Grid>
        <sf:TabControlExt Name="tabControlExt" 
                          ItemsSource="{Binding TabItems}" SelectedItem="{Binding SelectedTabItem, Mode=TwoWay}"
                          AllowDragDrop="False" TabItemHeaderStyle="Underline" Margin="0,1,0,0"
                          ShowTabListContextMenu="False" CloseMode="Delete">
            <sf:TabControlExt.ItemTemplate>
                <DataTemplate>
                    <sf:TabItemExt Header="{Binding Header}">
                    </sf:TabItemExt>
                </DataTemplate>
            </sf:TabControlExt.ItemTemplate>

            <sf:TabControlExt.ContentTemplate>
                <DataTemplate>
                    <ContentPresenter Content="{Binding Content}"/>
                </DataTemplate>
            </sf:TabControlExt.ContentTemplate>
        </sf:TabControlExt>
    </Grid>
</UserControl>
