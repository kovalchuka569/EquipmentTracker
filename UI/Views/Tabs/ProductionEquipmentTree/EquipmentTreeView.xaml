<UserControl x:Class="UI.Views.Tabs.ProductionEquipmentTree.EquipmentTreeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:sf="http://schemas.syncfusion.com/wpf"
             xmlns:models="clr-namespace:Core.Models.Tabs.ProductionEquipmentTree;assembly=Core"  
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True"
             mc:Ignorable="d"
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:behavior="clr-namespace:Core.Behaviors.SyncfusionTreeView;assembly=Core">
    
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
    </UserControl.Resources>
    <Grid>
        <sf:SfTreeView 
            x:Name="treeView"
            VerticalAlignment="Top" HorizontalAlignment="Left"
            BorderThickness="1"
            FocusVisualStyle="{x:Null}"
            IsAnimationEnabled="True"
            AllowEditing="True"
            EditTrigger="F2"
            ShowLines="True"
            Loaded="TreeView_Loaded"
            AllowDeleting="True"
            ItemsSource="{Binding Folders}"
            SelectedItem="{Binding SelectedFolder, Mode=TwoWay}">
            
            <i:Interaction.Behaviors>
                <behavior:SfTreeViewEditBehavior ItemBeginEditCommand="{Binding ItemBeginEditCommand}"
                                                 ItemEndEditCommand="{Binding ItemEndEditCommand}"/>
            </i:Interaction.Behaviors>
            
            <i:Interaction.Triggers>
                
                
                <i:EventTrigger EventName="ItemDoubleTapped">
                    <prism:InvokeCommandAction Command="{Binding OpenFileCommand}" CommandParameter="{Binding SelectedItem, ElementName=treeView}" />
                </i:EventTrigger>
                
            </i:Interaction.Triggers>
            
            
            <sf:SfTreeView.HierarchyPropertyDescriptors>
                <sf:HierarchyPropertyDescriptor ChildPropertyName="SubFolders" TargetType="{x:Type models:Folder}"/></sf:SfTreeView.HierarchyPropertyDescriptors>
            
            <sf:SfTreeView.ItemTemplate>
                <DataTemplate>
                    <Grid>
                        
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition/>
                        </Grid.ColumnDefinitions>
                        
                        <Image
                            Width="20"  
                            Height="20"
                            HorizontalAlignment="Stretch"
                            VerticalAlignment="Center"
                            Source="{Binding ImageIcon}" />
                        
                        <TextBlock VerticalAlignment="Center"
                                   FontSize="15"
                                   Grid.Column="1"
                                   Padding="5 5"
                                   Text="{Binding FileName}">
                            <TextBlock.InputBindings>
                                <MouseBinding Gesture="LeftDoubleClick" Command="{Binding OpenFileCommand}"/>
                            </TextBlock.InputBindings>
                        </TextBlock>
                        
                    </Grid>
                </DataTemplate>
            </sf:SfTreeView.ItemTemplate>
            
           
            
            <sf:SfTreeView.EditTemplate>
                <DataTemplate>
                    
                    <TextBox Text="{Binding FileName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                             FontSize="13"/>
                    
                </DataTemplate>
            </sf:SfTreeView.EditTemplate>
            
                <sf:SfTreeView.ContextMenu>
                    <ContextMenu>
                        <MenuItem Header="Створити нову категорію" 
                                  Command="{Binding AddCategoryCommand}"
                                  Visibility="Visible"/>
                        
                        <MenuItem Header="Створити нову підкатегорію" 
                                  Command="{Binding AddSubCategoryCommand}" 
                                  CommandParameter="{Binding SelectedFolder}"
                                  Visibility="{Binding CanAddSubCategory, Converter={StaticResource BoolToVisibilityConverter}}"/>
                        
                        <MenuItem Header="Редагувати"
                                  Command="{Binding EditCommand}"
                                  Visibility="{Binding CanEdit, Converter={StaticResource BoolToVisibilityConverter}}"/>
                                    
                        <MenuItem Header="Видалити" 
                                  Command="{Binding DeleteCommand}" 
                                  CommandParameter="{Binding SelectedFolder}"
                                  Visibility="{Binding CanDeleteCategory, Converter={StaticResource BoolToVisibilityConverter}}"/>
                    </ContextMenu>
                </sf:SfTreeView.ContextMenu>
            
        </sf:SfTreeView>
        
    </Grid>
</UserControl>
